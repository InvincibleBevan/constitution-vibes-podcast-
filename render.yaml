services:
  - type: web
    name: constitution-vibes
    env: python
    rootDir: constitution-vibes-podcast- 
    buildCommand: "pip install -r requirements.txt && cd frontend && npm install && npm run build"
    startCommand: "python app.py"
    plan: free
